services:
  app_proxy:
    environment:
      APP_HOST: acalatrava-luckyminer_server_1
      APP_PORT: 3000

  server:
    image: ghcr.io/acalatrava/luckyminer:sha-b57b61f
    init: true
    volumes:
      - ${APP_DATA_DIR}/data:/app/data
    devices:
      - /dev/dri:/dev/dri
    privileged: true
    environment:
      - NODE_ENV=production
